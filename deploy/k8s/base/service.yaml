apiVersion: v1
kind: Service
metadata:
  name: filesync-api
spec:
  selector:
    app: filesync
    component: api
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  type: ClusterIP
